info:
  name: document/discard
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/document/discard?oid={{expectedDocumentId}}"
  params:
    - name: oid
      value: "{{expectedDocumentId}}"
      type: query
  body:
    type: json
    data: |-
      {
        "oid": "{{expectedDocumentId}}"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.body.uuAppErrorMap
      operator: isEmpty

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
