info:
  name: document/moveToTrashByOid
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/document/moveToTrashByOid?oid={{expectedDocumentId}}"
  params:
    - name: oid
      value: "{{expectedDocumentId}}"
      type: query
  body:
    type: json
    data: |-
      {
        "oid": "{{expectedDocumentId}}"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.body.oid
      operator: eq
      value: "{{expectedDocumentId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
