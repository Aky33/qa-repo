info:
  name: document/get
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/document/get?oid={{expectedDocumentId}}"
  params:
    - name: oid
      value: "{{expectedDocumentId}}"
      type: query
  auth: inherit

runtime:
  assertions:
    - expression: ""
      operator: eq
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
